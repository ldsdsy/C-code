#include <iostream>
#include<cstring>
using namespace std;

 void f(char a[],int k)             //k为关注点
    {
        if(k==strlen(a))
            cout<<a<<endl;
        for(int i=k;i<strlen(a);i++)
        {
            char t=a[k]; a[k]=a[i]; a[i]=t;
            f(a,k+1);
            t=a[k]; a[k]=a[i]; a[i]=t;
        }


    }


int main()
{
    char a[100];
    cin>>a;

    f(a,0);
    return 0;
}
